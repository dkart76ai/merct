use both together

packetViewer2 (server)
packetviewer (client)

server will stream binary(logs) files to client

client will decode and show

binary files are located in capturePackets/logs

generated by the game scanner playwright bot