LOGI{"user_id":"tb:86287335","nickname":"Thordirn","profile_url":"","require_auth_for_profile_image":false,"ekey":"_wmTg2Tr3Ns_TKMQQHlHHyz62wbyzZCwraA-QTj6xSAwarxZERCsJTgS4hhWKLH-cnE1TewbrrKKgNfZA2QuWg","metadata":{"color":"0","hideTitlesTs":"0","memberInfo":"131,131000046f2,1310000000b,1,5,NPC","titles":""},"is_hide_me_from_friends":false,"ping_interval":15,"pong_timeout":5,"login_ts":1775323790287,"reconnect":{"max_interval":20,"interval":2,"retry_cnt":-1,"mul":2},"bc_duration":0,"preferred_languages":[],"max_unread_cnt_on_super_group":100,"unread_cnt_threading_policy":0,"last_msg_threading_policy":0,"use_reaction":true,"disable_supergroup_mack":false,"multiple_file_send_max_size":30,"request_dedup_interval_ms":500,"allow_sdk_log_ingestion":true,"log_publish_config":{"default":{"min_stat_count":100,"min_interval":10800,"max_stat_count_per_request":1000,"lower_threshold":10,"request_delay_range":180},"realtime":{"min_stat_count":1,"min_interval":0,"max_stat_count_per_request":1000,"lower_threshold":0,"request_delay_range":20}},"key":"16b172061f69386c71ad5a92fb689a0d1559c5f2","expires_at":-1,"unread_cnt":{"all":5,"ts":1775323790300},"discovery_keys":[],"file_upload_size_limit":25,"premium_feature_list":["ai_chatbot","chat_uikit_sdk_access","ai_engine","data_export","moderation_open","moderation_group","bot_interface","profanity_filter","domain_filter","analytics","analytics_v2","super_group_channel","reactions","salesforce_extension_ai"],"emoji_hash":"","notifications":{"enabled":false,"feed_channels":{},"template_list_token":"0","settings_updated_at":0},"message_template":{"template_list_token":"0"},"ai_agent":{"template_list_token":"efdb58d726939d8626f9cfbe2f0fdb2c6524d9ef89a8640314824690fd283ee3"},"application_attributes":["enable_message_threading","reactions","enable_og_tag","use_last_seen_at","allow_group_channel_create_from_sdk","allow_group_channel_invite_from_sdk","allow_group_channel_leave_from_sdk","allow_group_channel_update_from_sdk","allow_group_channel_delete_from_sdk","allow_non_operators_to_mention_channel","allow_only_operator_sdk_to_update_group_channel_name","allow_only_operator_sdk_to_update_group_channel_image","allow_operators_to_edit_operators","allow_operators_to_ban_operators","allow_message_update_from_sdk","allow_anonymous_message_update","allow_message_delete_from_sdk","allow_operator_message_delete","allow_migration_api","allow_file_messages_from_sdk","allow_sdk_file_message_urls","allow_non_owner_message_fetch_from_sdk","allow_emoji_create_from_sdk","allow_emoji_update_from_sdk","allow_emoji_delete_from_sdk","allow_emoji_category_create_from_sdk","allow_emoji_category_update_from_sdk","allow_emoji_category_delete_from_sdk","allow_emoji_update_from_different_user","allow_emoji_delete_from_different_user","allow_emoji_category_update_from_different_user","allow_emoji_category_delete_from_different_user","allow_aiagent_channels_in_moderation_dashboard","allow_sdk_noti_stats_log_publish","allow_sdk_feature_local_cache_log_publish","allow_sdk_ai_agent_stats_log_publish"],"sdk_background_connection_close_timeout":30,"config_sync_needed":false,"services":["chat"]}


SEND
MESG{"channel_url":"sendbird_group_channel_805436316_2fe967ce5ba3fcc3dc550f4a26d5b5b43a71a53d","message":"/%0%/","data":"{\"subs\":{\"/%0%/\":{\"type\":\"coord\",\"entryType\":\"user\",\"x\":888,\"y\":588,\"realmId\":305,\"staticId\":2,\"name\":\"Zamath\",\"v\":1}}}","custom_type":"user","mention_type":"users","mentioned_user_ids":[],"reply_to_channel":false,"req_id":"rq-e4d5d330-c43a-4e3b-81a3-18ca3a13d5e8","pin_message":false}


RECV
MESG{"channel_id":3257458870,"scrap_id":"","user":{"name":"Thordirn","image":"","require_auth_for_profile_image":false,"guest_id":"tb:86287335","id":805436316,"role":"operator","metadata":{"color":"0","hideTitlesTs":"0","memberInfo":"131,131000046f2,1310000000b,1,5,NPC","titles":""},"is_bot":false,"is_ai_bot":false,"is_active":true},"silent":false,"check_reactions":false,"is_op_msg":true,"is_guest_msg":true,"data":"{\"subs\":{\"\/%0%\/\":{\"type\":\"coord\",\"entryType\":\"user\",\"x\":888,\"y\":588,\"realmId\":305,\"staticId\":2,\"name\":\"Zamath\",\"v\":1}}}","sts":1775323968329,"channel_url":"sendbird_group_channel_805436316_2fe967ce5ba3fcc3dc550f4a26d5b5b43a71a53d","channel_type":"group","is_super":true,"mention_type":"users","mentioned_users":[],"message_events":{"send_push_notification":"receivers","update_unread_count":true,"update_mention_count":true,"update_last_message":true},"message_retention_hour":-1,"request_id":"rq-e4d5d330-c43a-4e3b-81a3-18ca3a13d5e8","previous_message_id":null,"translations":{},"custom_type":"user","is_removed":false,"last_updated_at":1775323968329,"message":"\/%0%\/","msg_id":14729429128,"ts":1775323968329,"req_id":"rq-e4d5d330-c43a-4e3b-81a3-18ca3a13d5e8","unread_cnt":{"all":3,"ts":1775323968388}}


staticId: 2 — player city (entryType: "user")

staticId: 6 — tile (entryType: "tile") — from the earlier captured message

staticId: 400 — mercenary exchange (entryType: "poi")



wss://journal-feed.totalbattle.com//?token=eyJ0eXAiOiJKV1QiLCJraWQiOiJrZXlpZCIsImFsZyI6IkhTMjU2In0%3d.eyJhdWQiOiJKb3VybmFsIiwiZXhwIjoxNzc4NDYyNzY2LCJuYW1lIjoiMTMwOTk2NTA0MzQ0MiIsInJvbGUiOiJQbGF5ZXIifQ%3d%3d.RFzB4PExt009gYjbWRMbETUqh4K7Mlk%2fiM7QS5qriUY%3d


async function scan(k, x, y) {
  const sessionToken = [0x69, 0xD1, 0x25, 0x34, 0xF7, 0x58, 0x20, 0x39, 0x18, 0x55, 0xCC, 0x75]
  const gameServer = 'game-us47.totalbattle.com'

  const tileId = x * 1000 + y

  const payload = [
    0x94,
    0xCD, 0xF4, 0x01,  // viewX = 500
    0xCD, 0xF4, 0x01,  // viewY = 500
    0x92,
    // tile entry
    0x91, 0xCF,
    tileId & 0xFF, (tileId >> 8) & 0xFF, (tileId >> 16) & 0xFF, (tileId >> 24) & 0xFF,
    k & 0xFF, (k >> 8) & 0xFF, (k >> 16) & 0xFF, (k >> 24) & 0xFF,
    // session token
    0xC4, 0x0C, ...sessionToken
  ]

  const header = new Uint8Array(8)
  const view = new DataView(header.buffer)
  view.setUint32(0, payload.length + 8, true)
  view.setUint32(4, payload.length, true)

  const body = new Uint8Array([...header, ...payload])

  const response = await fetch(`https://${gameServer}/rubens-realm${k}`, {
    method: 'POST',
    headers: {
      'accept': '*/*',
      'content-type': 'application/octet-stream',
      'Referer': 'https://totalbattle.com/'
    },
    body
  })

  const buffer = await response.arrayBuffer()
  const bytes = new Uint8Array(buffer)
  const text = new TextDecoder('utf-8').decode(bytes)

  // extract tb: player IDs
  const playerIds = [...text.matchAll(/tb:(\d+)/g)].map(m => m[0])

  // extract tile coordinates from 91 CF pattern
  const tiles = []
  for (let i = 0; i < bytes.length - 9; i++) {
    if (bytes[i] === 0x91 && bytes[i+1] === 0xCF) {
      const tileId = bytes[i+2] | (bytes[i+3] << 8) | (bytes[i+4] << 16) | (bytes[i+5] << 24)
      const kingdom = bytes[i+6] | (bytes[i+7] << 8) | (bytes[i+8] << 16) | (bytes[i+9] << 24)
      const tx = Math.floor(tileId / 1000)
      const ty = tileId % 1000
      if (tx <= 999 && ty <= 999 && kingdom > 0 && kingdom < 10000) {
        tiles.push({ k: kingdom, x: tx, y: ty })
      }
    }
  }

  // print hex dump of full response for analysis
  const hex = [...bytes].map(b => b.toString(16).padStart(2, '0')).join(' ')

  console.log('=== SCAN RESULT ===')
  console.log(`Tile: K:${k} X:${x} Y:${y}`)
  console.log('Players found:', playerIds.length ? playerIds : 'none')
  console.log('Tiles in response:', tiles)
  console.log('Raw hex:', hex)
  console.log('Raw text:', text)

  return { playerIds, tiles, hex, text }
}



corrected version

async function scan(k, x, y) {
  const sessionToken = [0x69, 0xD1, 0x25, 0x34, 0xF7, 0x58, 0x20, 0x39, 0x18, 0x55, 0xCC, 0x75]
  const gameServer = 'game-us47.totalbattle.com'

  const tileId = x * 1000 + y

  const payload = [
    0x94,                          // fixarray[4] — 4 top-level elements
    0xCD, 0xF4, 0x01,              // viewX = 500
    0xCD, 0xF4, 0x01,              // viewY = 500
    0x92,                          // fixarray[2] — [tiles, token]
      0x91,                        // fixarray[1] — 1 tile
        0x91, 0xCF,                // fixarray[1] containing uint64
        tileId & 0xFF, (tileId >> 8) & 0xFF, (tileId >> 16) & 0xFF, (tileId >> 24) & 0xFF,
        k & 0xFF, (k >> 8) & 0xFF, (k >> 16) & 0xFF, (k >> 24) & 0xFF,
      0xC4, 0x0C, ...sessionToken  // session token
  ]

  const header = new Uint8Array(8)
  const dv = new DataView(header.buffer)
  dv.setUint32(0, payload.length + 8, true)
  dv.setUint32(4, payload.length, true)

  const body = new Uint8Array([...header, ...payload])

  const response = await fetch(`https://${gameServer}/rubens-realm${k}`, {
    method: 'POST',
    headers: {
      'accept': '*/*',
      'content-type': 'application/octet-stream',
      'Referer': 'https://totalbattle.com/'
    },
    body
  })

  const buffer = await response.arrayBuffer()
  const bytes = new Uint8Array(buffer)
  const text = new TextDecoder('utf-8').decode(bytes)

  const playerIds = [...text.matchAll(/tb:(\d+)/g)].map(m => m[0])
  const hex = [...bytes].map(b => b.toString(16).padStart(2, '0')).join(' ')

  console.log('Players:', playerIds.length ? playerIds : 'none')
  console.log('Text:', text)
  console.log('Hex:', hex)

  return { playerIds, hex, text }
}


-- 2nd round with 8 payloads tested and compared

94           fixarray[4]
CD XX XX     f1 (viewport)
CD XX XX     f2 (viewport)
92           fixarray[2]  ← [tile_list, token_wrapper]
  91         fixarray[1]  ← tile_list with 1 tile
    CF XX XX XX XX XX XX XX XX  ← uint64 tile
  C4 0C XX...  ← token
a0           fixarray[0]  ← empty array (4th element of the outer 94)
90           fixarray[0]  ← ???
9d / 99      fixarray[13/9] ← ???


--

94           fixarray[4]
  CD XX XX   element 1: f1
  CD XX XX   element 2: f2
  92         element 3: fixarray[2] = [tiles, token]
    91 CF... tiles
    C4 0C... token
  a0         element 4: fixarray[0] = empty
90 9d        ← these are OUTSIDE the 94, separate data

--

[8-byte header]
94           outer fixarray[4]
  CD XX XX   viewport x as tileId
  CD XX XX   viewport y as tileId
  92         fixarray[2]
    91       fixarray[1] — tile list
      CF ... tile uint64
    C4 0C .. session token
  a0         fixarray[0] — always empty
[extra section — separate msgpack data]


-- final ?

async function scan(k, x, y) {
  const token = [0x69, 0xD1, 0x4A, 0x76, 0xF7, 0x58, 0x20, 0x39, 0x18, 0x5C, 0x62, 0x53]
  const gameServer = 'game-us47.totalbattle.com'
  const tileId = x * 1000 + y
  const f1 = 0x013E  // viewport x
  const f2 = 0x0906  // viewport y

  const payloadBytes = [
    0x94,
    0xCD, f1 & 0xFF, (f1>>8) & 0xFF,
    0xCD, f2 & 0xFF, (f2>>8) & 0xFF,
    0x92,
      0x91,
        0xCF,
        tileId & 0xFF, (tileId>>8) & 0xFF, (tileId>>16) & 0xFF, (tileId>>24) & 0xFF,
        k & 0xFF, (k>>8) & 0xFF, (k>>16) & 0xFF, (k>>24) & 0xFF,
      0xC4, 0x0C, ...token,
    0xa0,
  ]

  const payload = new Uint8Array(payloadBytes)
  const header = new Uint8Array(8)
  const dv = new DataView(header.buffer)
  dv.setUint32(0, payload.length + 8, true)
  dv.setUint32(4, payload.length, true)

  const body = new Uint8Array([...header, ...payload])

  const response = await fetch(`https://${gameServer}/rubens-realm${k}`, {
    method: 'POST',
    headers: { 'accept': '*/*', 'content-type': 'application/octet-stream', 'Referer': 'https://totalbattle.com/' },
    body
  })

  const buf = new Uint8Array(await response.arrayBuffer())
  const text = new TextDecoder().decode(buf)
  const playerIds = [...text.matchAll(/tb:\d+/g)].map(m => m[0])
  const hex = [...buf].map(b => b.toString(16).padStart(2,'0')).join(' ')

  console.log('Players:', playerIds.length ? playerIds : 'none')
  console.log('Hex:', hex)
  console.log('Text:', text)
  return { playerIds, hex, text }
}

